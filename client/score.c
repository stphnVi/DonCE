#include "constants.h"

/*
void setScore(SDL_Renderer *renderer, char *fruit)
{

    TTF_Font *font = TTF_OpenFont("./Assets/arcadeFont.ttf", tamaño);
    if (font == NULL)
    {
        printf("No se pudo cargar la fuente: %s\n", TTF_GetError());
        // Manejo de error...
    }
}

*/